export class RookRules {
  static directionIsAllowed(piece: any, move: any): boolean {
    return true;
  }
  
  static MoveIsAllowedOnboard(piece: any, move: any, board: any): boolean {
    return true;
  }
}
